A. Script
    1. DM.cs
        a. public enum PSVへ追加。
        b. convertPsvSkillStr2Enum(string name)のreturn 部分へ追加。
        c. convertPsvSkillStr2Enum(string name)の2番目、string[]部分へ追加。

    2. Player.cs
        a. [Header("<---- PASSIVE SKILL ---->")]へ変数を追加。
        b. #region Set PSV Skill Dataの //* Initへ追加。
        c. #region Set PSV Skill Dataの //* Applyへ追加。

    3. Skill_Infomation.cs
        a. class ItemPsvListの //* valueへ追加。
        b. class PsvSkillの getPsvInfo2Str(Player pl)へ追加。
        c. class PsvSkillの getPsvLVList(Player pl)へ追加。
        d. class PsvSkillの getPsvMaxLVList(Player pl)へ追加。

    4. LevelUpPanelAnimate.cs  
        a. onClickSkillUpBtn()へ追加。

    5. Language.cs
        a. public enum TXTへ追加。
        b. //* PSVで、言語のstring[]配列宣言。
        c. getTxt(string name)へ追加。


B. Editor(hierarchy)
    1.Prefab
        a. Model/Player/Model/Prefab/全てOBJECT：Item Infoスクリプトで「Item Passive」の▼Arrへ追加。
        b. Model/Bat/Prefab/全てOBJECT：Item Infoスクリプトで「Item Passive」の▼Arrへ追加。

    2. Home Scene
        // Game全般に使うPsv Image
        a. rsc / UI / Prefab / PassiveSkillImgListへオブジェクトPrefab生成。
        b. DataManagerのDMスクリプトで、Item Passiveの▼Arrへ「a.で生成したPrefab」追加。

        // Psv Info PANELに使う、テーブル情報
        c. rsc / Home / SelectItemPanel / Content / PsvInfoへオブジェクトPrefab生成。 
        d. Prefabに張ってあるItemInfoスクリプトで、Name Txtsに言語作成。(Name Txts、Explain Txts)
        e. DataManagerのDMスクリプトで、scrollviewsのPsvInfoのItemPrefsへ「c.で生成したPrefab」追加。


    3. Play Scene
        a. GameManagerのスクリプトPASSIVE TABLEで、Psv Skill Img Prefsへ追加。
    


